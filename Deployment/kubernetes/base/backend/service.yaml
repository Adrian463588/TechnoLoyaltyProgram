apiVersion: v1
kind: Service
metadata:
  name: loyalty-backend
  labels:
    app: loyalty-backend
spec:
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: loyalty-backend
  type: ClusterIP
