apiVersion: v1
kind: Service
metadata:
  name: loyalty-frontend
  labels:
    app: loyalty-frontend
spec:
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: loyalty-frontend
  type: ClusterIP
